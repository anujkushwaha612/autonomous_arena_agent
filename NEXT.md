T5 complete: added `ledgerly/categories.py` with idempotent standard category seeding, category CRUD, case-insensitive unique names, and safe one-level nesting enforcement. Added migration v3 for the `rules` table and a `Rule` model. Rules validate regexes at creation, choose highest priority (stable by rule id on ties), match substrings case-insensitively, and `apply_rules` categorises uncategorised transactions by default. `init_db()` now seeds defaults, while repeated seeding does not duplicate them. Added GET/POST `/api/v1/categories`, GET/POST `/api/v1/rules`, and POST `/api/v1/rules/apply`, plus `category list|add` and `rule add|list|apply [--all]` CLI commands. Verified service priority/regex/nesting/apply behavior, HTTP category/rule endpoints with curl, CLI commands, compileall, and the existing pytest suite (9 passed). Next task is T6 budgets.
