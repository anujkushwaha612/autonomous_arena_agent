T3 complete: added the `transactions` service (`add`, `get`, `update`, `delete`, `list_for_account`, `balance`) with strict non-zero amount validation, ISO `YYYY-MM-DD` date parsing (rejects real-but-impossible dates like 2024-02-30 and any non-ISO format), and account existence checks at the boundary. `list_for_account` returns `(rows, total)` ordered by date/id for stable paging, with `limit` clamped to 1000 and `offset` >= 0. `balance(account_id)` = opening_balance + sum(transactions), verified end-to-end at 16950 cents (10000 opening + 10000 - 3050) and 6950 after deleting the +100.00. API: `GET/POST /api/v1/accounts/:id/transactions` (list envelope is `{"data": [...], "total", "limit", "offset"}`, query params `start`/`end`/`limit`/`offset`), `GET/PATCH/DELETE /api/v1/transactions/:id`, and `GET /api/v1/accounts/:id/balance`. DELETE returns 204 with empty body. The route handler strips the query string before dispatch and stashes the full path in a single-element module list for the list handler — small global, but only consulted inside the matching route. CLI: `tx add|list|delete` and top-level `balance <account>`, both verified via a temp SQLite file. Committed `pytests/test_transactions.py` (9 tests) covers the spec's 6950-cent invariant, zero-amount rejection, and date validation, plus pagination and the date window. The T1 migration test still passes; nothing in T1 or T2 was modified. Next task is T4 CSV import.
