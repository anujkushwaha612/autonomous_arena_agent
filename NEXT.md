T2 complete: added validated account create/get/list/update/archive operations, case-insensitive duplicate handling, exact Decimal-to-cents conversion, a v2 `archived_at` migration, all account API routes, and `account add|list|show|archive` CLI commands. Account records retain archived history and expose `archived`/`archived_at`; archived accounts remain visible in `list_all`. Curl verification covered 201 creation, 409 duplicates, 400 invalid kinds, PATCH/archive, and exact 12345-cent storage. Next task is T3 transactions.
