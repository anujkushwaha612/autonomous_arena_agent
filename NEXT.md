T4 complete: added `ledgerly/importer.py` with CSV delimiter/header sniffing, suggested mappings for amount or debit+credit columns, per-row parse errors, amount parsing for `(45.00)`, `1,234.56`, `1.234,56`, and trailing CR/DR, plus idempotent `import_rows` dedupe by `external_id` or `(date, description, amount_cents)`. API now supports `POST /api/v1/accounts/:id/import` as raw UTF-8 CSV text and returns parsed/imported/skipped/errors; the handler reads text only for that route so existing JSON routes stay unchanged. CLI now has `import <account> <file.csv> [--dry-run]` with a summary table; dry-run parses only, while real imports use the default dedupe. Verified parser examples, CLI dry-run/import/re-import, API import/re-import with curl, `python3 cli.py --help`, and the project check command (`cd app && python3 -m compileall -q . && python3 -m pytest pytests -q`). Next task is T5 categories and auto-categorisation rules.
