{
  "name": "impostor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.js --watch & tsx watch src/server/index.ts",
    "build": "node esbuild.config.js",
    "start": "npm run build && tsx src/server/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13"
  }
}
