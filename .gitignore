node_modules/
.arena-profile/
app/node_modules/

# runtime patch drops (raw agent payloads) — never commit
fastcapture/drops/*
!fastcapture/drops/.gitkeep

# transient
.drop-*.patch
.drop-*.bundle
*.log
.DS_Store
package-lock.json
app/package-lock.json
